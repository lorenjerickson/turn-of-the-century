# TODO

- Add a campaign log that captures all significant game events so that a summary of each encounter, scenario and campaign can be generated from the event stream. The log must include every interaction from every player and the GM, including chat messages, and record these as an event stream which will be used to generate a narrative description for later compilation into a book.
- fix action target selection 
- verify actions that cause damage/effects
- fully equip starter actors
- ghost trail representing a players plan (reflect attack, dodge, other actions with iconography)
- when an action is selected that requires a target, default to target selection rather than requiring a click in the plan
- group actions by type (movement, item, etc.)
- conditionally dodge in a chosen direction, 1-5 fail, 6-12 success in an randomm direction, 12-20 success in chosen direction
- scale map/movement to 5 ft
- to-hit and damage roles are not being prompted
- 
