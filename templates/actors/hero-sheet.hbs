<form class="{{cssClass}} totc-actor-sheet" autocomplete="off">
    <div class="totc-victorian-frame">
    <header class="sheet-header">
        <img class="profile-img" src="{{actor.img}}" data-edit="img" title="{{actor.name}}" height="104" width="104" />
        <section class="header-fields">
            <h1 class="charname">
                <input name="name" type="text" value="{{actor.name}}" placeholder="Name" />
            </h1>
            <div class="actor-subtitle">
                <span>{{localize "TOTC.Actor.Type"}}: {{actor.type}}</span>
                <span>{{localize "TOTC.Actor.Category"}}: {{system.classification.category}}</span>
                <span>{{localize "TOTC.Actor.Level"}}: {{system.progression.level}}</span>
            </div>
        </section>
    </header>

    <section class="sheet-body sheet-body--sidebar">
        <aside class="sheet-sidebar">
            <section class="totc-ability-scores">
                <div class="totc-ability-score">
                    <div class="totc-ability-score__value">{{system.abilities.str.value}}</div>
                    <div class="totc-ability-score__label">{{localize "TOTC.Actor.Ability.ShortStr"}}</div>
                    <div class="totc-ability-score__modifier">{{numberFormat system.abilities.str.bonus decimals=0 sign=true}}</div>
                </div>
                <div class="totc-ability-score">
                    <div class="totc-ability-score__value">{{system.abilities.dex.value}}</div>
                    <div class="totc-ability-score__label">{{localize "TOTC.Actor.Ability.ShortDex"}}</div>
                    <div class="totc-ability-score__modifier">{{numberFormat system.abilities.dex.bonus decimals=0 sign=true}}</div>
                </div>
                <div class="totc-ability-score">
                    <div class="totc-ability-score__value">{{system.abilities.con.value}}</div>
                    <div class="totc-ability-score__label">{{localize "TOTC.Actor.Ability.ShortCon"}}</div>
                    <div class="totc-ability-score__modifier">{{numberFormat system.abilities.con.bonus decimals=0 sign=true}}</div>
                </div>
                <div class="totc-ability-score">
                    <div class="totc-ability-score__value">{{system.abilities.int.value}}</div>
                    <div class="totc-ability-score__label">{{localize "TOTC.Actor.Ability.ShortInt"}}</div>
                    <div class="totc-ability-score__modifier">{{numberFormat system.abilities.int.bonus decimals=0 sign=true}}</div>
                </div>
                <div class="totc-ability-score">
                    <div class="totc-ability-score__value">{{system.abilities.wis.value}}</div>
                    <div class="totc-ability-score__label">{{localize "TOTC.Actor.Ability.ShortWis"}}</div>
                    <div class="totc-ability-score__modifier">{{numberFormat system.abilities.wis.bonus decimals=0 sign=true}}</div>
                </div>
                <div class="totc-ability-score">
                    <div class="totc-ability-score__value">{{system.abilities.cha.value}}</div>
                    <div class="totc-ability-score__label">{{localize "TOTC.Actor.Ability.ShortCha"}}</div>
                    <div class="totc-ability-score__modifier">{{numberFormat system.abilities.cha.bonus decimals=0 sign=true}}</div>
                </div>
                <div class="totc-ability-score">
                    <div class="totc-ability-score__value">{{system.abilities.san.value}}</div>
                    <div class="totc-ability-score__label">{{localize "TOTC.Actor.Ability.ShortSan"}}</div>
                    <div class="totc-ability-score__modifier">{{numberFormat system.abilities.san.bonus decimals=0 sign=true}}</div>
                </div>
            </section>
        </aside>

        <main class="sheet-main">
            <nav class="sheet-tabs tabs" data-group="primary">
                <a class="item" data-tab="profile">{{localize "TOTC.Sheet.Profile"}}</a>
                <a class="item" data-tab="inventory">{{localize "TOTC.Sheet.Inventory"}}</a>
                <a class="item" data-tab="encounter">{{localize "TOTC.Sheet.Encounter"}}</a>
                <a class="item" data-tab="identity">{{localize "TOTC.Actor.Identity"}}</a>
                <a class="item" data-tab="hero">{{localize "TOTC.Actor.TypeDetails"}}</a>
            </nav>

            <div class="tab profile" data-group="primary" data-tab="profile">
                <div class="totc-two-column">
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Profession"}}</label>
                        <select name="system.classification.profession">
                            <option value=""></option>
                            {{#each professionOptions as |profession|}}
                                <option value="{{profession.value}}" {{#if (eq ../system.classification.profession profession.value)}}selected{{/if}}>{{profession.label}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Origin"}}</label>
                        <input name="system.classification.origin" type="text" value="{{system.classification.origin}}" />
                    </div>
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Faction"}}</label>
                        <input name="system.profile.faction" type="text" value="{{system.profile.faction}}" />
                    </div>
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Role"}}</label>
                        <input name="system.profile.role" type="text" value="{{system.profile.role}}" />
                    </div>
                </div>
                <div class="form-group stacked">
                    <label>{{localize "TOTC.Actor.ProfileSummary"}}</label>
                    <textarea name="system.profile.summary" rows="3">{{system.profile.summary}}</textarea>
                </div>
                <div class="form-group stacked">
                    <label>{{localize "TOTC.Actor.Tags"}}</label>
                    <input name="_array.system.profile.tags" type="text" value="{{profileTags}}" placeholder="investigator, elite, london" />
                </div>
                <div class="form-group stacked">
                    <label>{{localize "TOTC.Actor.Biography"}}</label>
                    {{editor system.biography target="system.biography" button=true owner=owner editable=editable}}
                </div>
                <div class="form-group stacked">
                    <label>{{localize "TOTC.Actor.Notes"}}</label>
                    {{editor system.notes target="system.notes" button=true owner=owner editable=editable}}
                </div>

            </div>

            <div class="tab inventory" data-group="primary" data-tab="inventory">
                <section class="totc-inventory-group">
                    <h2>{{localize "TOTC.Actor.Equipment.Label"}}</h2>
                    <div class="totc-inventory-slots">
                        {{#each inventorySummary.equippedBySlot as |slot|}}
                            <div class="totc-inventory-slot">
                                <div class="totc-inventory-slot__label">{{slot.label}}</div>
                                {{#if slot.equippedItems.length}}
                                    <ul class="totc-inventory-items">
                                        {{#each slot.equippedItems as |item|}}
                                            <li class="totc-inventory-item">
                                                <img src="{{item.img}}" alt="{{item.name}}" />
                                                <span>{{item.name}} ({{item.type}})</span>
                                            </li>
                                        {{/each}}
                                    </ul>
                                {{else}}
                                    <p class="totc-equipment-slot__empty">{{localize "TOTC.Actor.Equipment.None"}}</p>
                                {{/if}}
                            </div>
                        {{/each}}
                    </div>
                </section>

                <section class="totc-inventory-group">
                    <h2>{{localize "TOTC.Actor.Pack.Label"}}</h2>
                    {{#if inventorySummary.packItems.length}}
                        <ul class="totc-inventory-items">
                            {{#each inventorySummary.packItems as |item|}}
                                <li class="totc-inventory-item">
                                    <img src="{{item.img}}" alt="{{item.name}}" />
                                    <span>{{item.name}} ({{item.type}})</span>
                                </li>
                            {{/each}}
                        </ul>
                    {{else}}
                        <p class="totc-equipment-slot__empty">{{localize "TOTC.Actor.Pack.Empty"}}</p>
                    {{/if}}
                </section>

                <section class="totc-inventory-group">
                    <h2>{{localize "TOTC.Actor.Inventory.Unassigned"}}</h2>
                    {{#if inventorySummary.otherItems.length}}
                        <ul class="totc-inventory-items">
                            {{#each inventorySummary.otherItems as |item|}}
                                <li class="totc-inventory-item">
                                    <img src="{{item.img}}" alt="{{item.name}}" />
                                    <span>{{item.name}} ({{item.type}})</span>
                                </li>
                            {{/each}}
                        </ul>
                    {{else}}
                        <p class="totc-equipment-slot__empty">{{localize "TOTC.Actor.Inventory.None"}}</p>
                    {{/if}}
                </section>
            </div>

            <div class="tab encounter" data-group="primary" data-tab="encounter">
                <section class="totc-inventory-group totc-encounter-planner">
                    <h2>{{localize "TOTC.Encounter.Planner"}}</h2>
                    {{#if encounterPlanner}}
                        <div class="totc-encounter-header">
                            <div class="totc-encounter-header__round">{{localize "TOTC.Encounter.Round"}} {{encounterPlanner.round}}</div>
                            <div class="totc-encounter-header__phase">{{localize "TOTC.Encounter.Phase"}}: {{encounterPlanner.phase}}</div>
                            <div class="totc-encounter-header__timer">{{localize "TOTC.Encounter.PlanningRemaining"}}: {{encounterPlanner.planningRemainingSeconds}}s</div>
                        </div>
                        <div class="totc-encounter-meter-row">
                            <label>{{localize "TOTC.Encounter.PlannedAp"}}: {{encounterPlanner.plannedAp}} / {{encounterPlanner.apBudget}}</label>
                            <progress max="{{encounterPlanner.apBudget}}" value="{{encounterPlanner.plannedAp}}"></progress>
                            <button
                                type="button"
                                data-action="totc-encounter-toggle-ready"
                                data-combatant-id="{{encounterPlanner.combatantId}}"
                                data-ready="{{encounterPlanner.ready}}"
                                {{#unless encounterPlanner.canCommit}}disabled{{/unless}}
                            >
                                {{#if encounterPlanner.ready}}
                                    {{localize "TOTC.Encounter.Committed"}}
                                {{else}}
                                    {{localize "TOTC.Encounter.CommitPlan"}}
                                {{/if}}
                            </button>
                        </div>
                        <div class="totc-encounter-meta">
                            <span>{{localize "TOTC.Encounter.SpentAp"}}: {{encounterPlanner.spentAp}}</span>
                            <span>{{localize "TOTC.Encounter.RemainingAp"}}: {{encounterPlanner.remainingAp}}</span>
                            <span>{{localize "TOTC.Encounter.CommitProgress"}}: {{encounterPlanner.committedCount}} / {{encounterPlanner.combatantCount}}</span>
                        </div>
                        {{#unless encounterPlanner.initiativeReady}}
                            <p class="totc-encounter-warning">{{localize "TOTC.Encounter.InitiativeRequired"}}</p>
                            {{#if encounterPlanner.canRollInitiative}}
                                <button type="button" data-action="totc-roll-initiative" data-combat-id="{{encounterPlanner.combatId}}" data-combatant-id="{{encounterPlanner.combatantId}}">{{localize "TOTC.Encounter.RollInitiative"}}</button>
                            {{/if}}
                        {{/unless}}
                        {{#if encounterPlanner.planningWarningActive}}
                            <p class="totc-encounter-warning">{{localize "TOTC.Encounter.PlanningWarning"}}</p>
                        {{/if}}
                        {{#if encounterPlanner.initiativeReady}}
                        <div class="totc-encounter-dual-list">
                            <div class="totc-encounter-listbox">
                                <h3>{{localize "TOTC.Encounter.AvailableActions"}}</h3>
                                <select class="totc-encounter-action-select" size="8" {{#unless encounterPlanner.canEditPlan}}disabled{{/unless}}>
                                    {{#each encounterPlanner.availableActions as |action|}}
                                        <option
                                            value="{{action.id}}"
                                            data-id="{{action.id}}"
                                            data-action-id="{{action.actionId}}"
                                            data-type="{{action.type}}"
                                            data-label="{{action.label}}"
                                            data-ap-cost="{{action.apCost}}"
                                            data-ap-min="{{action.apMin}}"
                                            data-ap-max="{{action.apMax}}"
                                            data-variable-ap="{{action.variableAp}}"
                                            data-requires-to-hit="{{action.requiresToHit}}"
                                            data-to-hit-bonus="{{action.toHitBonus}}"
                                            data-item-id="{{action.itemId}}"
                                            data-movement-feet="{{action.movementFeet}}"
                                            data-movement-feet-per-ap="{{action.movementFeetPerAp}}"
                                        >{{action.label}} ({{action.apCost}} AP)</option>
                                    {{/each}}
                                </select>
                                <select class="totc-encounter-target-select" {{#unless encounterPlanner.canEditPlan}}disabled{{/unless}}>
                                    <option value="">{{localize "TOTC.Encounter.Target.None"}}</option>
                                    {{#each encounterPlanner.targetOptions as |target|}}
                                        <option value="{{target.id}}">{{target.name}}</option>
                                    {{/each}}
                                </select>
                                <div class="totc-encounter-plan-controls">
                                    <label>
                                        {{localize "TOTC.Encounter.ActionPoints"}}
                                        <input class="totc-encounter-ap-input" type="number" value="1" min="1" max="6" step="1" disabled {{#unless encounterPlanner.canEditPlan}}disabled{{/unless}} />
                                    </label>
                                    <button type="button" data-action="totc-encounter-add-action" data-combatant-id="{{encounterPlanner.combatantId}}" {{#unless encounterPlanner.canEditPlan}}disabled{{/unless}}>{{localize "TOTC.Encounter.AddAction"}}</button>
                                    <button type="button" data-action="totc-encounter-clear-plan" data-combatant-id="{{encounterPlanner.combatantId}}" {{#unless encounterPlanner.canEditPlan}}disabled{{/unless}}>{{localize "TOTC.Encounter.ClearPlan"}}</button>
                                </div>
                            </div>
                            <div class="totc-encounter-listbox">
                                <h3>{{localize "TOTC.Encounter.PlannedActions"}}</h3>
                                {{#if encounterPlanner.queue.length}}
                                    <ol class="totc-encounter-queue">
                                        {{#each encounterPlanner.queue as |action actionIndex|}}
                                            <li>
                                                <div class="totc-encounter-queue-row">
                                                    <span>{{action.label}} ({{action.apCost}} AP)</span>
                                                    <button type="button" data-action="totc-encounter-remove-action" data-combatant-id="{{../encounterPlanner.combatantId}}" data-action-index="{{actionIndex}}" {{#unless ../encounterPlanner.canEditPlan}}disabled{{/unless}}>{{localize "TOTC.Encounter.RemoveAction"}}</button>
                                                </div>
                                                {{#if action.variableAp}}
                                                    <label class="totc-encounter-action-slider">
                                                        {{localize "TOTC.Encounter.ActionPoints"}}
                                                        <input
                                                            type="range"
                                                            min="{{action.apMin}}"
                                                            max="{{action.apMax}}"
                                                            step="1"
                                                            value="{{action.apCost}}"
                                                            data-action="totc-encounter-set-ap"
                                                            data-combatant-id="{{../encounterPlanner.combatantId}}"
                                                            data-action-index="{{actionIndex}}"
                                                            {{#unless ../encounterPlanner.canEditPlan}}disabled{{/unless}}
                                                        />
                                                        <span>{{action.apCost}}</span>
                                                    </label>
                                                {{/if}}
                                            </li>
                                        {{/each}}
                                    </ol>
                                {{else}}
                                    <p class="totc-pack-empty">{{localize "TOTC.Encounter.NoPlannedActions"}}</p>
                                {{/if}}
                            </div>
                        </div>
                        {{/if}}
                    {{else}}
                        <p class="totc-pack-empty">{{localize "TOTC.Encounter.NotInitialized"}}</p>
                    {{/if}}
                </section>
            </div>

            <div class="tab identity" data-group="primary" data-tab="identity">
                <div class="totc-two-column">
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Species"}}</label>
                        <input name="system.classification.species" type="text" value="{{system.classification.species}}" />
                    </div>
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Ancestry"}}</label>
                        <input name="system.classification.ancestry" type="text" value="{{system.classification.ancestry}}" />
                    </div>
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Size"}}</label>
                        <select name="system.classification.size">
                            <option value="tiny" {{#if (eq system.classification.size "tiny")}}selected{{/if}}>{{localize "TOTC.Actor.SizeTiny"}}</option>
                            <option value="sm" {{#if (eq system.classification.size "sm")}}selected{{/if}}>{{localize "TOTC.Actor.SizeSmall"}}</option>
                            <option value="med" {{#if (eq system.classification.size "med")}}selected{{/if}}>{{localize "TOTC.Actor.SizeMedium"}}</option>
                            <option value="lg" {{#if (eq system.classification.size "lg")}}selected{{/if}}>{{localize "TOTC.Actor.SizeLarge"}}</option>
                            <option value="huge" {{#if (eq system.classification.size "huge")}}selected{{/if}}>{{localize "TOTC.Actor.SizeHuge"}}</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Level"}}</label>
                        <input name="system.progression.level" type="number" value="{{system.progression.level}}" min="1" step="1" />
                    </div>
                </div>
            </div>

            <div class="tab hero" data-group="primary" data-tab="hero">
                <div class="totc-two-column">
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Hero.Archetype"}}</label>
                        <input name="system.hero.archetype" type="text" value="{{system.hero.archetype}}" />
                    </div>
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Hero.Rank"}}</label>
                        <input name="system.hero.rank" type="text" value="{{system.hero.rank}}" />
                    </div>
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.Hero.Renown"}}</label>
                        <input name="system.hero.renown" type="number" value="{{system.hero.renown}}" min="0" step="1" />
                    </div>
                    <div class="form-group">
                        <label>{{localize "TOTC.Actor.ProficiencyBonus"}}</label>
                        <input name="system.progression.proficiencyBonus" type="number" value="{{system.progression.proficiencyBonus}}" min="1" step="1" />
                    </div>
                </div>
                <div class="form-group stacked">
                    <label>{{localize "TOTC.Actor.Hero.Bonds"}}</label>
                    <input name="_array.system.hero.bonds" type="text" value="{{heroBonds}}" placeholder="Detective Club, Crown Office" />
                </div>
            </div>
        </main>
    </section>
    </div>
</form>
